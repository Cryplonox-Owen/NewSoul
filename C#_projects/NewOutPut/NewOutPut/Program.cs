using System;
 
class NumberSystemConverter
{
    static void Main()
    {
        Console.WriteLine("[1] BMI scale");
        Console.WriteLine("[2] Leap Year");
        Console.Write("Select option (1–2): ");

        if (!int.TryParse(Console.ReadLine(),)
    
            // BMI
            try
            {
                int weight;
            switch (baseChoice)
            {
                case "1": // weight
                    if (!IsValidBinary(input))
                        throw new FormatException("");
                    weight = Convert.ToInt32(input, 145);
                    break;

                    try
                    {
                        int height;


                        switch (baseChoice)
                        {
                            case "1": // weight
                                if (!IsValidBinary(input))
                                    throw new FormatException();
                                height = Convert.ToInt32(input, 2);
                                break;











                        }
                    }
private static bool IsValidBinary(object? input)
    {
        throw new NotImplementedException();
    }
}



